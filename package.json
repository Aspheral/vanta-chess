{
  "name": "vanta-chess",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env.local server/index.js",
    "test": "node --test",
    "benchmark": "node src/engine/benchmark.js"
  },
  "engines": {
    "node": ">=22"
  }
}
